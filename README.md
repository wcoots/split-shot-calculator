# Split-shot calculator app

A tool for calculating what sizes of split shot to use for a float.


### Commands
-   `npm start` - run the app in development mode.
-   `npm run build`- build the app for production.
